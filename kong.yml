_format_version: "1.1"

services:
  - name: my-service
    url: http://httpbin.org
    routes:
      - name: my-route-d
        paths:
          - /my-path
      - name: my-route-d
        paths:
          - /test
