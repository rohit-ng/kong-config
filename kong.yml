_format_version: "1.1"

services:
  - name: my-service
    url: http://httpbin.org
    routes:
      - name: my-route-d1
        paths:
          - /my-path
      - name: my-route-d2
        paths:
          - /test
